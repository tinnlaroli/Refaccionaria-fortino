import { defineConfig } from "vite";

// Stub de configuración Vite/PWA — la app POS se implementará en apps/pos (feat/ux)
export default defineConfig({
  root: ".",
  build: {
    outDir: "dist",
  },
});
