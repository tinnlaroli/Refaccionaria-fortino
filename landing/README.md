# Landing — Refaccionaria Fortino

**Rama:** `feat/landing`

Sitio publico React: inicio, catalogo, contacto.

``bash
git checkout feat/landing
npm install
npm run dev
``

Catálogo usa API publica (rama `feat/api`).
